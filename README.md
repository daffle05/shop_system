# Shop System — Group Task 4 (Member 1)

This repo contains the Payment interface and Processor consumer.
Run: `dart run main.dart` (after other members add implementations or use the DummyPayment).
